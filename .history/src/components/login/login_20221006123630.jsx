import React from 'react';

const Login = (props) => {
    return (
        <section>
            <Header></Header>
            <Footer></Footer>
        </section>
    )
}
            
    );

export default Login;